<div class="panel panel-default">
    <div class="panel-heading">
        <%= yield(:form_title) %>
    </div>
    <div class="panel-body">
        <%= simple_form_for([@group, @publication]) do |f| %>
        <div class="row">
            <div class="col-md-12">
                <%= f.error_notification %>
                <%= f.input :title, label: "Title", autofocus: true %>
                <%= f.input :event, label: "Event" %>
                <%= f.input :description, label: "Description", input_html: {rows: 10} %>
                <hr>
                <%= f.button :submit, class: "form-control btn btn-primary" %>
            </div>
        </div>
        <% end %>
    </div>
</div>
